<div align="center">
  <%= will_paginate comments %> <!-- because the @comments is passed in as object: @comments, we can now simply refer to the object as "comments" (Rails magic) -->
</div>
<% comments.each do |comment| %>
  <%= render(partial: 'comments/comment', object: comment) %>
<% end %>
<div align="center">
  <%= will_paginate comments %>
</div>
